# data/

- `raw/location_aware_gis_leakage_dataset.csv` — baseline dataset copied verbatim from
  `Sukrut10k/Smart-Water-Leak-Detection` (5 000 rows × 13 cols, sha256 `9d7758a4…`). Treated as immutable.
- `raw/testing.csv` — 2-row sample without the label, from the same repo.
- `processed/` — git-ignored; derived datasets go here in later phases.

Field documentation: `docs/DATA_DICTIONARY.md`. Quality report: `reports/data_quality_report.md`.
