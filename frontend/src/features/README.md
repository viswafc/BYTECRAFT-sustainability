# features/

Feature modules (state + components + services scoped to one intelligence capability) go here from
Phase 3 onward, e.g. `features/telemetry`, `features/baseline`, `features/incidents`. Pages compose features.
